<nav class="top-bar">
 <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to "Movie Marathon Helper", root_path %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

	<section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
     
    </ul>

    <!-- Right Nav Section -->
    <ul class="right">
   		<li class="divider"></li>
      <li><%= link_to "Home", root_path%></li>
      <li class="divider"></li>
      <li><%= link_to "Help", help_path %></li>
      <li class="divider"></li>
      <li><%= link_to "Marathon Men", users_path %></li>
      <% if current_user %>
      <li class="divider"></li>
      <li><%= link_to "Movies", movies_path %></li>
      <% end %>
      <li class="divider"></li>
      <li class="has-form">
          <div class="row collapse">
            <div class="small-8 columns">
              <%= form_tag controller:"movies", action: "movie_search" do %>
                <%= text_field_tag :movie, nil, placeholder: 'Movie title or IMDB id' %>
            </div>
            <div class="small-4 columns">
                <%= submit_tag "Search movie", class: "alert button" %>
            </div>
            <% end %>
          </div>
        
      </li>
      <li class="divider show-for-small"></li>
      <% if current_user %>
      <li class="has-dropdown"><a href="#">Account</a>
        <ul class="dropdown">
          <li><%= link_to "Log out", logout_path %></li>
          <li><%= link_to "Edit account", edit_user_path(current_user) %></li>
        </ul>
      </li>
      <% else %>
      <li><%= link_to "Create an account", new_user_path %></li>
      <li><%= link_to "Log in", new_session_path%></li>
       <% end %>
    </ul>
  </section>
</nav>